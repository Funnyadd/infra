apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: staging
resources:
  - ../../base
patchesStrategicMerge:
  - patch-images.yaml
  - patch-ingress.yaml
  - patch-secrets.yaml
configMapGenerator:
  - name: app-config
    literals:
      - APP_ENV=staging
generatorOptions:
  disableNameSuffixHash: true
