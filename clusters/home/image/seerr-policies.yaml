apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: seerr-prod
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: seerr
  filterTags:
    pattern: '^v(?P<ver>3\.\d+\.\d+)$'
    extract: '$ver'
  policy:
    semver:
      range: ">=3.0.0 <4.0.0"
