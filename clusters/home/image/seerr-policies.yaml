apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: seerr-prod
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: seerr
  filterTags:
    pattern: '^(?P<ver>1\.\d+\.\d+)$'
    extract: '$ver'
  policy:
    semver:
      range: "*"
