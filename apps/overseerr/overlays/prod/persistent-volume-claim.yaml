apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: overseerr-persistent-volume-claim
  namespace: prod
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: overseerr-manual
  resources:
    requests:
      storage: 10Gi
