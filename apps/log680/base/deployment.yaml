apiVersion: apps/v1
kind: Deployment
metadata:
  name: log680
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: metrics
          image: docker.io/funnyadd/metrics-a25-grp1-eq10:DEBUG
          envFrom:
            - secretRef:
              name: app-secrets
        # - name: mobilitysoft
        #   image: docker.io/funnyadd/mobilitysoft:DEBUG
        #   ports: [{ name: http-mobilitysoft, containerPort: 5002 }]
        #   # envFrom:
        #   #   - secretRef: { name: app-secrets }
